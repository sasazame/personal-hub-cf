name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run type checking
        run: pnpm typecheck

      - name: Run linting
        run: pnpm lint

      - name: Run tests with coverage
        run: pnpm test -- --coverage

      - name: Run integration tests
        run: pnpm --filter @personal-hub/backend test:integration

      - name: Upload coverage reports
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: apps/backend/coverage/

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm build

      - name: Verify build output
        run: |
          # Check that TypeScript compilation succeeded
          test -d apps/backend/dist || (echo "Backend build failed" && exit 1)
          test -d apps/frontend/dist || (echo "Frontend build failed" && exit 1)

  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers and dependencies
        run: |
          pnpm playwright install --with-deps chromium
          # Install additional system dependencies if needed
          sudo apt-get update || true
          sudo apt-get install -y libnss3 libnspr4 libatk1.0-0 libatk-bridge2.0-0 libatspi2.0-0 || true

      - name: Build frontend
        run: |
          cd apps/frontend
          VITE_API_BASE_URL=http://localhost:8788 pnpm build

      - name: Setup backend environment
        run: |
          cd apps/backend
          # Create .dev.vars file for CI environment
          cat > .dev.vars << EOF
          JWT_SECRET=test-jwt-secret-for-ci-only
          OAUTH_GITHUB_CLIENT_ID=test-github-client-id
          OAUTH_GITHUB_CLIENT_SECRET=test-github-client-secret
          OAUTH_GOOGLE_CLIENT_ID=test-google-client-id
          OAUTH_GOOGLE_CLIENT_SECRET=test-google-client-secret
          EOF
          
      - name: Initialize database
        run: |
          cd apps/backend
          pnpm wrangler d1 migrations apply personal-hub-local --local || echo "Some migrations already applied"
      
      - name: Start backend server
        run: |
          cd apps/backend
          pnpm wrangler dev --local --test-scheduled --port 8788 &
          BACKEND_PID=$!
          echo "BACKEND_PID=$BACKEND_PID" >> $GITHUB_ENV
          # Wait for backend to be ready
          for i in {1..30}; do
            if curl -f http://localhost:8788/health > /dev/null 2>&1; then
              echo "Backend is ready!"
              break
            fi
            echo "Waiting for backend... ($i/30)"
            sleep 1
          done

      - name: Run E2E tests
        run: |
          cd apps/frontend
          # Start preview server and capture the actual port
          pnpm vite preview --port 5173 > /tmp/vite.log 2>&1 &
          FRONTEND_PID=$!
          echo "FRONTEND_PID=$FRONTEND_PID" >> $GITHUB_ENV
          cd ../..
          
          # Wait for vite to start and output the URL
          echo "Waiting for vite preview server to start..."
          for i in {1..10}; do
            if grep -q "Local:" /tmp/vite.log; then
              echo "Vite server started"
              break
            fi
            echo "Waiting... ($i/10)"
            sleep 1
          done
          
          # Debug: Show vite output
          echo "Vite output:"
          cat /tmp/vite.log
          
          # Extract the actual port from vite output - updated pattern to match actual format
          ACTUAL_PORT=$(grep -oP 'http://localhost:\K\d+' /tmp/vite.log | head -1)
          
          # Fallback to default port if extraction fails
          if [ -z "$ACTUAL_PORT" ]; then
            echo "Port extraction failed, using default port 5173"
            ACTUAL_PORT=5173
          fi
          
          echo "Frontend running on port: $ACTUAL_PORT"
          
          # Verify the server is accessible
          curl -f http://localhost:$ACTUAL_PORT || (echo "Frontend server not accessible" && exit 1)
          
          # Run E2E tests
          SKIP_WEBSERVER=1 E2E_BASE_URL=http://localhost:$ACTUAL_PORT VITE_API_BASE_URL=http://localhost:8788 pnpm playwright test --config=playwright.ci.config.ts --reporter=html

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      - name: Cleanup
        if: always()
        run: |
          if [ ! -z "$BACKEND_PID" ]; then kill $BACKEND_PID || true; fi
          if [ ! -z "$FRONTEND_PID" ]; then kill $FRONTEND_PID || true; fi